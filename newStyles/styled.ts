import styled from 'styled-components'
import { flexbox } from './utils'

export const StyledWncgStakingPage = styled.div`
  ${flexbox('space-between')}
  width: 100%;
  max-width: 1440px;
  min-height: 100%;
  margin: 0 auto;
`
